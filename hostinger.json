{
  "framework": "express",
  "nodeVersion": "20",
  "buildCommand": "npm ci && npm run build",
  "startCommand": "npm start",
  "entryFile": "app.cjs",
  "note": "Do NOT set Output directory to dist for static-only hosting. This app serves dist via Express (npm start)."
}
