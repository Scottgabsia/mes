{
  "framework": "angular",
  "nodeVersion": "20",
  "buildCommand": "npm ci && npm run build",
  "startCommand": "npm start",
  "entryFile": "app.cjs",
  "outputDirectory": "dist/browser",
  "note": "React+Vite app using Angular-style dist/browser. Entry app.cjs runs Express (API + SPA). Override ng build with npm run build in hPanel if needed."
}
